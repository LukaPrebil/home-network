---
# Hermes role handlers

- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart Hermes gateway
  ansible.builtin.systemd:
    name: "{{ hermes_service_name }}"
    state: restarted
