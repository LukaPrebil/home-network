---
# Main tasks file for OTBR role
# Deploys OpenThread Border Router

- name: Include prerequisite tasks
  ansible.builtin.include_tasks: prerequisites.yml
  tags:
    - otbr
    - prerequisites
    - setup

- name: Include directory setup tasks
  ansible.builtin.include_tasks: directories.yml
  tags:
    - otbr
    - directories
    - setup

- name: Include deployment tasks
  ansible.builtin.include_tasks: deploy.yml
  tags:
    - otbr
    - deploy

- name: Include dataset seed tasks
  ansible.builtin.include_tasks: seed.yml
  tags:
    - otbr
    - otbr-seed

- name: Include auto-recovery watchdog tasks
  ansible.builtin.include_tasks: watchdog.yml
  tags:
    - otbr
    - otbr-watchdog
