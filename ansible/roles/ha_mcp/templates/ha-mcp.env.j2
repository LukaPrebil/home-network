HOMEASSISTANT_URL={{ ha_mcp_homeassistant_url }}
HOMEASSISTANT_TOKEN={{ ha_mcp_token | replace('$', '$$') }}
# The URL path IS the credential (upstream standard mode: URL-path secrecy).
# It also gates the settings UI, which mounts under this path.
MCP_SECRET_PATH={{ ha_mcp_secret_path }}
